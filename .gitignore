node_modules
dist
dist-electron
dist-installer
dist-installer*/
.vite
.DS_Store
*.log
data/
quarantine/
certs/
staging-source/
.fusion/
# JDK that the Oracle Java VS Code extension auto-installs (don't commit ~300MB of JDK)
**/oracleJdk-*/
docs/fusion/oracleJdk-*/
# claw-bridge extension build output / installed deps
claw-bridge/node_modules/
claw-bridge/out/

# VS Code extension package artifact
*.vsix

# fleet-managed local instruction files (never ship to GitHub)
CLAUDE.md
.claude/
AGENTS.md

# local secrets - none tracked today, this is a safety net against ever adding one
.env
.env.*
!.env.example
